# Evidence Map (Seccion -> Evidencia)

## Mapa principal

| Seccion de tesis | Afirmacion/tema clave | Archivos fuente que sustentan | Resultados/tablas/graficos disponibles | Vacio detectado |
|---|---|---|---|---|
| Resumen | Existe un servidor de prediccion con API REST + MCP, integrable con Onyx | `README.md`, `server/interface/http/api.py`, `docker-compose.yml`, `.docs/onyx_integration.md` | Endpoints definidos (`/health`, `/predict`, `/mcp`), arquitectura de despliegue documentada | No hay KPI final de negocio/modelo en un artefacto unico de resumen ejecutivo |
| Introduccion | Caso de uso: prediccion de demanda/stock en supermercado sobre datos historicos Kaggle | `.notebooks/Global_Simple_LightGBM_Timeseries_Forecast_Final_academic.ipynb` (celdas 5, 44, 47), `data/train.csv`, `data/test.csv`, `.docs/precompute_features_pipeline.md` | Dimensiones de dataset, pipeline de prediccion y archivo submission | Falta documento explicito de formulacion del problema y objetivos medibles con metricas finales |
| Estado del arte / conceptos teoricos | Se discuten conceptos de training-serving skew, ONNX serving, LightGBM/MLForecast y decisiones de arquitectura | `.docs/precompute_features_pipeline.md`, `.docs/data_serving_architecture.md`, `.docs/Tesis Maestria Ciencia de Datos Levy Gossweiler.docx` | Texto conceptual y motivacion tecnica | No se encontro bibliografia estructurada (`.bib`/gestor de referencias) ni matriz formal de trabajos relacionados |
| Metodologia (datos y features) | Pipeline de features de 30 variables replicado para produccion | `scripts/precompute_features.py`, `.docs/precompute_features_pipeline.md`, `.docs/data_serving_architecture.md`, notebook academico celdas 9, 15, 17, 25, 31 | Lista y orden de 30 features, estrategia de escalado (`LocalStandardScaler`), feriados seleccionados, lags/rolling | Falta evidencia exportada de validacion numerica "entrenamiento vs serving" en un test de regresion de predicciones |
| EDA | Se realizaron exploraciones tabulares y seleccion de feriados por correlacion | notebook academico celdas 13, 15, 18 | Tabla de holidays (`Number of holidays: 103`), tabla de feriados seleccionados por correlacion | No hay figuras EDA exportadas (PNG/SVG) ni reporte EDA independiente versionado |
| Experimentos (modelado) | Se entreno LightGBM global con validacion temporal; tambien existen resultados baseline | notebook academico celdas 23, 25, 35, 38, 40; `.notebooks/cv_results_baseline_retrained.csv`; `.notebooks/submission.csv` | Config CV (h=16, windows=5, step=120), feature importance top-10, archivo baseline CV (SeasonalNaive/AutoETS), submission final | RMSLE LGBM no queda trazable por error de sintaxis en celda 35; no archivo exportado `cv_results_lgbm.csv` |
| Implementacion del sistema | Implementacion Clean Architecture con puertos/adaptadores y backend ONNX/dummy | `server/domain/*`, `server/application/*`, `server/infrastructure/*`, `server/interface/http/*`, `README.md`, `ONNX_INTEGRATION_SUMMARY.md` | Estructura en capas, adaptadores `ONNXModel`, `DummyModel`, `ProductionPreprocessor/Postprocessor` | Parte de la documentacion de arquitectura esta desalineada con el estado actual (diagramas y texto legado de agente conversacional custom) |
| Infraestructura y despliegue | Existe despliegue docker-compose multi-servicio y scripts operativos | `Dockerfile`, `docker-compose.yml`, `.env.example`, `scripts/containers.sh`, `scripts/e2e_validate.sh`, `.github/workflows/*.yml` | Definicion de servicios, variables de entorno, smoke tests, CI lint/test/typecheck/security/deploy | No hay evidencia versionada de ejecucion real E2E en ambiente final (logs/resultados archivados) |
| Validacion tecnica y calidad | Hay suite de tests amplia y cobertura alta reportable | `server/tests/*.py`, `.github/workflows/test.yml`, ejecucion local auditada de `pytest` | 183 tests pasan; cobertura total reportada 98% (corrida local con `--cov=server`) | Corrida completa falla en 10 pruebas MCP parametrizadas con `trio` por dependencia faltante; pipeline puede quedar no reproducible segun entorno |
| Resultados de API | API y MCP responden bajo backend dummy; contratos de entrada/salida definidos | `server/interface/http/schemas.py`, `server/tests/test_api.py`, `server/tests/test_integration.py`, `server/tests/test_mcp.py` | Validaciones HTTP, contratos Pydantic, pruebas de flujo completo dummy | `openapi.json` en la raiz no contiene un schema valido (archivo con mensaje de error), por lo que no sirve como evidencia documental de contrato |
| Conclusiones | Hay material para conclusiones tecnicas (trade-offs, limitaciones operativas) | `.docs/precompute_features_pipeline.md` (secciones de limitaciones e incidencias), `.docs/project_completion_phases.md` | Riesgos OOM, decisiones de worker unico, uso de parquet precomputado | No existe documento consolidado de conclusiones finales con hallazgos cuantificados del modelo principal |

## Vacios transversales prioritarios

1. Falta metrica final reproducible del modelo LightGBM principal (RMSLE/MAE/RMSE exportada desde notebook o script).
2. Falta consolidar un artefacto unico de resultados finales (tabla comparativa baseline vs modelo final).
3. Falta estandarizar reproduccion de notebooks (rutas de datos, dependencia exacta, ejecucion limpia sin outputs stale).
4. Falta alinear documentacion tecnica legacy con arquitectura realmente desplegada.
5. Falta un archivo OpenAPI valido versionado para soporte documental de contrato API.

